#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, and
 *	the alphabet in uppercase right after.
 *
 *
 * Return: 0 if succesful
 */

int main(void)
{
	char lower = 'a';
	char upper = 'A';

	for (lower <= z; lower++)
	{
		putchar(lower);
	}

	for (upper <= Z; upper++)
	{
		putchar(upper);
	}

	putchar('\n');
	return (0);

}
